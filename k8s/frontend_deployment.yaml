apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: null
  generation: 1
  labels:
    run: hello-frontend
  name: hello-frontend
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/hello-frontend
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      run: hello-frontend
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: hello-frontend
    spec:
      containers:
      - image: hello-grpc-client
        imagePullPolicy: Never
        name: hello-frontend
      restartPolicy: Always
