apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: null
  generation: 1
  labels:
    run: hello-backend
  name: hello-backend
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/hello-backend
spec:
  progressDeadlineSeconds: 2147483647
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      run: hello-backend
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: hello-backend
    spec:
      containers:
      - image: hello-grpc-server
        imagePullPolicy: Never
        name: hello-backend
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    run: hello-backend
  name: hello-backend
  selfLink: /api/v1/namespaces/default/services/hello-backend
spec:
  ports:
  - name: grpc-web
    port: 50066
    protocol: TCP
    targetPort: 50066
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    run: hello-backend
